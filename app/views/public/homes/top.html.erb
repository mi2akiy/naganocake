  <div class="container">
  
    <div class="row form-inline">
      <!--サイドバー-->
      <div class="col-3">
        <table class="table table-hover">
          <thead>
            ジャンル検索
          </thead>
          <tbody>
            <% @genres.each do |genre| %>
              <tr>
                <td>
                  <%= genre.name %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
      
      <div class="col-9 borderd">
        ようこそ、ながのCAKEへ！<br>
        このさいとは、ケーキ販売のECサイトになります。<br>
        会員でない方も商品の閲覧はできますが、<br>
        購入には会員登録が必要になります。
      </div>
      
    </div>
    

    <h3>新着商品</h3>
    
    <div class="row form-inline">
     <div class="d-flex p-2 bd-highlight flex-wrap">
      <% @items.each do |item| %>
        <div class="item m-2">
          <div class="item-image">
            <%= link_to public_item_path(item.id) do %>
              <%= image_tag item.get_item_image(180,180) %>
            <% end %>
          </div>
          <div class="item-name">
            <%= link_to public_item_path(item.id) do %>
              <%= item.name %>
            <% end %>
          </div>
          <div class="item-price">
            ¥<%= item.price.to_s(:dalimited) %>
          </div>
        </div>
      <% end %>
    </div>
   </div>
    
    <div class="row">
      <div class="col-auto">
        <%= link_to "すべての商品を見る>", public_items_path %>
      </div>
    </div>
    </div>
    
    

      
  
  
  
  
  
  
  
  </div>